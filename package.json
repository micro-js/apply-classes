{
  "name": "@f/apply-classes",
  "description": "Apply classes to an element from an object map",
  "repository": "git://github.com/micro-js/apply-classes.git",
  "version": "1.0.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/add-class": "^1.0.0",
    "@f/has-class": "^0.1.1",
    "@f/remove-class": "^1.0.0"
  },
  "devDependencies": {
    "hihat": "^2.6.4",
    "tap-dev-tool": "^1.3.0",
    "tape": "^4.4.0"
  }
}
